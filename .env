
# ====================================================================
# Example Environment Variables
# ====================================================================
#
# - 이 파일을 `.env`로 복사한 후, 각 변수에 실제 값을 채워넣으세요.
# - `SecretStr`로 처리되는 값들은 코드 내에서 직접 노출되지 않습니다.
# - 주석 처리된 값들은 선택 사항입니다.

# --- Bybit API 설정 (필수) ---
# V5 Unified Trading 계정의 API 키와 시크릿을 입력하세요.
BYBIT_API_KEY="1JxwEdjbgBucLQHDrA"
BYBIT_API_SECRET="1mUiexpjddtNj06m3q0MvY1YalYVSaY3zJC9"
# 실전 매매를 하려면 false로 변경하세요. 기본값은 테스트넷입니다.
BYBIT_TESTNET=true

# --- JWT 및 인증 설정 (필수) ---
# JWT 토큰을 서명하는 데 사용될 비밀 키입니다. 복잡한 문자열로 변경하세요.
# (예: `openssl rand -hex 32` 명령어로 생성)
JWT_SECRET="change_this_secret_key_please"
# RN 앱에서 로그인할 때 사용할 관리자 계정 정보입니다.
ADMIN_USER="admin"
# 기본 비밀번호는 "admin"입니다. 아래는 bcrypt로 해시된 값입니다.
# 새 해시를 생성하려면 passlib를 사용하여 생성 후 붙여넣으세요.
ADMIN_PASS_HASH="$2b$12$X/VkwIMWDSoVp2eduVK1mex88NX2AmJGnDCYy.PeoAGC8oom2LpgK"

# --- 리스크 관리 설정 (기본값 사용 가능) ---
DAY_LOSS_LIMIT_USD=200
RISK_PER_TRADE=0.1 # 계좌 잔고의 95%를 한 거래의 리스크로 사용

# --- 트렌드 커넥터 API 키 (선택 사항) ---
# 아래 키들이 없으면 해당 커넥터는 모의(Mock) 모드로 동작합니다.
NEWS_API_KEY="c8aac850603343f8b8dee5e3e14eafd4"
X_BEARER_TOKEN="YOUR_X_API_V2_BEARER_TOKEN"
# FB_APP_ID="YOUR_FACEBOOK_APP_ID"
# FB_APP_SECRET="YOUR_FACEBOOK_APP_SECRET"

# --- 데이터베이스 및 이벤트 큐 설정 (기본값 사용 가능) ---
# 기본값: 프로젝트 루트에 trading_bot.db 라는 SQLite 파일 생성
DB_URL="sqlite+aiosqlite:///./trading_bot.db"
# PostgreSQL 사용 예시 (주석 해제 후 사용):
# DB_URL="postgresql+asyncpg://user:password@localhost:5432/trading_bot_db"

# Redis/Kafka 사용 예시 (주석 해제 후 사용):
# REDIS_URL="redis://localhost:6379"
# KAFKA_BROKER_URL="kafka://localhost:9092"

# --- 로깅 설정 ---
LOG_LEVEL="DEBUG"
LOG_TO_FILE=true
LOG_FILE_PATH="logs/app.log"
LOG_JSON_FORMAT=false

